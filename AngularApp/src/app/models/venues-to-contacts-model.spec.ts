import { VenuesToContactsModel } from './venues-to-contacts-model';

describe('VenuesToContactsModel', () => {
  it('should create an instance', () => {
    expect(new VenuesToContactsModel()).toBeTruthy();
  });
});
