import { CSharpDateToJsDate } from './csharp-date-to-js-date';

describe('CSharpDateToJsDate', () => {
  it('should create an instance', () => {
    expect(new CSharpDateToJsDate()).toBeTruthy();
  });
});
